apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: taskflow-prod-db-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault-secret-store-prod
    kind: ClusterSecretStore
  target:
    name: taskflow-prod-db-secret
    creationPolicy: Owner
  data:
    - secretKey: postgres-password
      remoteRef:
        key: postgres-password