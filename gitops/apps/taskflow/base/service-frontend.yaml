apiVersion: v1
kind: Service
metadata:
  name: taskflow-frontend
  labels:
    app: taskflow-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: taskflow-frontend
