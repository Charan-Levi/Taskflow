apiVersion: v1
kind: Service
metadata:
  name: taskflow-python
  labels:
    app: taskflow-python
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: taskflow-python
