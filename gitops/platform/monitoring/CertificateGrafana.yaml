apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-dev-tls
  namespace: monitoring
spec:
  secretName: grafana-dev-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - grafana-dev.taskflow.catlevi.online